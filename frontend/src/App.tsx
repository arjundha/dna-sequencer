import "./App.css";

function App() {
	// const [count, setCount] = useState(0);
	// <button onClick={() => setCount((count) => count + 1)}>count is {count}</button>

	return (
		<>
			<div>Hello!!!!!!!!</div>
		</>
	);
}

export default App;
